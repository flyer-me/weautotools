@model List<CloudWork.Model.ViewModels.UsersForRoleViewModel>

@{
	var roleId = ViewBag.roleId;
}

<form method="post">
	<div class="card">

		<div class="card-header">
			<h3>Role: @ViewBag.RollName </h3>
		</div>
		<div class="card-header">
			<h4>编辑Role内账户</h4>
		</div>
		<div class="card-body">
			@for (int i = 0; i < Model.Count(); i++)
			{
				<input type="hidden" asp-for="@Model[i].UserId" />
				<input type="hidden" asp-for="@Model[i].UserName" />
				<input asp-for="@Model[i].IsSelected" class="form-check-input" />
				<label class="form-check-label">@Model[i].UserName</label>
			}
		</div>
		<div class="card-footer">
			<input type="submit" value="提交" class="btn btn-primary"
				   style="width:auto" />
			<a asp-action="Edit" asp-route-id="@roleId"
			   class="btn btn-primary" style="width:auto">取消</a>
		</div>
	</div>
</form>