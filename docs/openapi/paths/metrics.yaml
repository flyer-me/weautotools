get:
  tags:
    - Admin
  summary: Prometheus 指标出口（文本格式）
  description: 返回 Prometheus 格式的指标（生产环境常由 JVM actuator 或 sidecar 提供，此处为便捷包装）。
  responses:
    '200':
      description: 指标文本
      content:
        text/plain:
          schema:
            type: string
